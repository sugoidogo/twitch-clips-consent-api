{
	"name": "twitch-clips-consent",
	"version": "1.0.1",
	"private": true,
	"scripts": {
		"deploy": "node scripts/build.mjs && wrangler deploy",
		"dev": "node scripts/serve-dev.mjs",
		"update": "npm update && wrangler types"
	},
	"devDependencies": {
		"@twurple/auth": "^7.4.0",
		"selflare": "^1.1.2",
		"typescript": "^5.5.2",
		"wrangler": "^4.38.0"
	}
}
